spring:
#datasource:
#  url: jdbc:h2:mem:testdb
#  driver-class-name: org.h2.Driver
#  username: gayoon
#  password: 1234
#  jpa:
#    database-platform: org.hibernate.dialect.H2Dialect
#  h2:
#    console:
#      enabled: true

  application:
    name: albaeaseTest
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/store
    username: root
    password: Dnjschs2151!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: mysql
    //database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: validate # api테스트용, 배포시 validate 수정예정
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
server:
  port: 8080

  #Swagger
springdoc:
    api-docs:
      enabled: true       # API 문서 생성을 활성화
      path: /v3/api-docs  # 기본 경로 설정
    swagger-ui:
      enabled: true        # Swagger UI 활성화
      path: /swagger-ui.html  # UI 경로 설정

logging:
  level:
    org.hibernate.SQL: debug
    com.example.albaease: debug


# JWT
#jwt:
#  secret: ${JWT_SECRET}
#  token-validity-in-seconds: 86400

# Redis
#  redis:
#    host: localhost
#    port: 6379

# WebSocket
#websocket:
#  allowed-origins: "*"
#  endpoint: /ws
#  destination-prefixes: /topic, /queue
